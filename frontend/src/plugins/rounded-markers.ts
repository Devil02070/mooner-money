// import { ISeriesApi } from "lightweight-charts";

// type CustomMarkerType = "B" | "S" | "DB" | "DS"
// type CustomMarker = {
//     type: CustomMarkerType;
// }
// export function createRoundedMarkerPlugin(series: ISeriesApi<"Custom">, markers: CustomMarker[]) {
//     return {
//         draw: (ctx: CanvasRenderingContext2D, pixelRatio: number) => {
//             const priceScale = series.priceScale();
//             // const timeScale = series.t
//             ctx.save();
//             markers.forEach((ca))
//         }
//     }
// } 